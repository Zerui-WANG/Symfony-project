<form method="post" action="{{ path('admin_effect_student_delete', {'id': effect_student.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ effect_student.id) }}">
    <button class="btn">Delete</button>
</form>
